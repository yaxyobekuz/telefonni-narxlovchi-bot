const { check_command } = require("../utils");

const airpods_steps = ({
  t,
  k,
  user,
  chat_id,
  message,
  user_state,
  get_state_name,
  get_state_data,
  user_state_data,
  check_state_name,
  update_state_name,
  update_state_data,
}) => {

};

module.exports = airpods_steps;
